type Nft @entity {
  id: ID!
  address: String!
  tokenId: BigInt!
  owner: Account!
  rolesHistory: [Role!] @derivedFrom(field: "nft")
}

type Account @entity {
  id: ID!
  nfts: [Nft!] @derivedFrom(field: "owner")
}

type Role @entity {
  id: ID!
  roleId: Bytes!
  nft: Nft!
  grantor: Account!
  grantee: Account!
  expirationDate: BigInt!
  data: Bytes!
}
