type Nft @entity {
  id: ID! # tokenAddress + tokenId
  address: String!
  tokenId: BigInt!
  owner: Account!
  rolesHistory: [Role!] @derivedFrom(field: "nft")
}

type Account @entity {
  id: ID! # address
  nfts: [Nft!] @derivedFrom(field: "owner")
}

type Role @entity {
  id: ID! # grantorAddress + tokenAddress + tokenId + granteeAddress + roleHash
  roleId: Bytes!
  nft: Nft!
  grantor: Account!
  grantee: Account!
  expirationDate: BigInt!
  data: Bytes!
}
